
  # Set up project layout

  This is a code bundle for Set up project layout. The original project is available at https://www.figma.com/design/mshZjyJExphFDLJZRxURBM/Set-up-project-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  